sudo apt update
sudp apt install mpich -y

mpicc fn.c -o fn

mpirun -np 4 ./fn



#include <mpi.h>
#include <stdio.h>

int main(int argc, char **argv)
{
    MPI_Init(&argc, &argv);

    int rank, size;
    MPI_Comm_rank(MPI_COMM_WORLD, &rank);
    MPI_Comm_size(MPI_COMM_WORLD, &size);

    printf("Hello from rank %d out of %d\n", rank, size);

    MPI_Finalize();
    return 0;
}



#include <mpi.h>
#include <stdio.h>

int main(int argc, char **argv) {
    int rank, size;
    int n = 8;
    int numbers[8] = {1, 2, 3, 4, 5, 6, 7, 8};
    int local_sum = 0, total_sum = 0;

    MPI_Init(&argc, &argv);
    MPI_Comm_rank(MPI_COMM_WORLD, &rank);
    MPI_Comm_size(MPI_COMM_WORLD, &size);

    // Divide array among processes
    for (int i = rank; i < n; i += size) {
        local_sum += numbers[i];
    }

    // Collect sums from all processes at root (rank 0)
    MPI_Reduce(&local_sum, &total_sum, 1, MPI_INT, MPI_SUM, 0, MPI_COMM_WORLD);

    if (rank == 0) {
        printf("Total sum of array = %d\n", total_sum);
    }

    MPI_Finalize();
    return 0;
}
